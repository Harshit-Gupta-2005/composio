---
"@composio/core": minor
---

Add `session.update()` method for partially updating session configuration after creation. Accepts the same camelCase config shape as `composio.create()` and mutates the session in-place.
